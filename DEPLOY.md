# 干快快小程序部署指南

## 前置条件

- ✅ 小程序已注册，AppID: `wxd77aeec77df9af5e`
- ✅ 小程序已备案、审核通过
- ⏳ 需要开通云开发

---

## 第一步：下载项目

1. 用微信开发者工具打开项目
2. 选择「导入项目」
3. 目录选择：`/workspace/leak-repair-miniapp`
4. AppID 已配置好，直接确认

---

## 第二步：开通云开发

1. 在微信开发者工具中，点击工具栏的「云开发」按钮
2. 首次开通会提示创建云开发环境
3. 填写环境名称（如：`prod`）
4. 选择基础版（免费额度足够 MVP 使用）
5. 等待环境创建完成

### 记录环境 ID

开通后，在云开发控制台可以看到环境 ID，类似：
```
cloud1-xxx
```

---

## 第三步：创建数据库集合

在云开发控制台 → 数据库，创建以下 3 个集合：

| 集合名 | 说明 | 权限设置 |
|--------|------|----------|
| `orders` | 订单数据 | 仅创建者可写，所有人可读 |
| `sources` | 来源/门店 | 仅创建者可写，所有人可读 |
| `admins` | 管理员 | 仅创建者可写，所有人可读 |

### 初始化管理员密码

在 `admins` 集合中，点击「添加记录」，插入：

```json
{
  "password": "你的管理密码",
  "createdAt": 1710000000000
}
```

> ⚠️ 密码建议使用 8 位以上数字字母组合

---

## 第四步：部署云函数

在微信开发者工具中：

1. 展开 `cloudfunctions` 目录
2. 依次右键点击每个云函数文件夹（login、order、source）
3. 选择「上传并部署：云端安装依赖」
4. 等待部署完成（每个约 30 秒）

部署成功后，云开发控制台 → 云函数 会显示 3 个函数。

---

## 第五步：本地调试

1. 点击开发者工具的「编译」按钮
2. 测试各功能：
   - 首页 → 提交需求 → 查看订单
   - 门店登录 → 查看订单
   - 管理后台 → 登录 → 订单管理/来源管理

---

## 第六步：上传代码

1. 点击工具栏「上传」按钮
2. 填写版本号：`1.0.0`
3. 填写备注：`MVP 首版上线`
4. 确认上传

---

## 第七步：提交审核

1. 登录 [微信公众平台](https://mp.weixin.qq.com)
2. 进入「管理」→「版本管理」
3. 找到刚上传的开发版本，点击「提交审核」
4. 填写审核信息：
   - 功能页面：首页、提交需求、我的订单、订单详情
   - 测试账号：无需提供（用户端无需登录）
5. 等待审核（通常 1-3 天）

---

## 第八步：发布上线

审核通过后：

1. 在微信公众平台「版本管理」中
2. 点击「全量发布」
3. 小程序正式上线！

---

## 生成门店二维码

上线后，为每个门店生成专属小程序码：

### 方法一：管理后台生成

1. 用管理员身份登录小程序
2. 进入「来源管理」→「创建来源」
3. 填写门店信息后保存
4. 点击「复制门店信息」获取 sourceId

### 方法二：使用微信官方工具

1. 登录 [微信公众平台](https://mp.weixin.qq.com)
2. 进入「工具」→「生成小程序码」
3. 路径填写：`pages/index/index?sourceId=STORE001`
4. 生成并下载图片

### 路径格式

```
pages/index/index?sourceId=门店编号
```

例如：
- `pages/index/index?sourceId=STORE001` - 门店001 的专属码
- `pages/index/index?sourceId=PARTNER002` - 合作伙伴002 的专属码

---

## 常见问题

### Q: 云函数调用失败？

检查云函数是否部署成功，环境 ID 是否正确。

### Q: 管理员登录失败？

1. 检查 `admins` 集合是否有记录
2. 检查密码是否正确
3. 首次登录后，openId 会自动绑定

### Q: 门店无法登录？

1. 在管理后台「来源管理」创建门店
2. 使用门店编号 + 验证码登录
3. 验证码可在来源管理中查看/修改

---

## 联系支持

如有问题，请在项目仓库提 Issue。
